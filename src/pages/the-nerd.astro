---
import Layout from "@/layouts/Layout.astro";
import NerdFeature from "@/components/Shared/UI/NerdFeature.astro";
import { nerdFacts } from "@/data";
---

<Layout title="The Nerd - Mainasara Olulanke">
  <main class="mx-auto max-w-screen-2xl pt-24 md:pt-28">
    <section class="px-4 text-center text-white lg:px-8">
      <h1
        class="mt-4 text-7xl leading-[0.85] font-normal tracking-tight sm:text-8xl md:text-[9rem] lg:text-[11rem]"
      >
        The Nerd<span class="text-background">.</span>
      </h1>
    </section>

    <div
      class="mt-8 flex flex-col gap-28 px-4 text-white md:mt-14 md:gap-40 lg:px-8"
    >
      {
        nerdFacts.map((fact, index) => (
          <NerdFeature
            index={index}
            label={fact.label}
            title={fact.title}
            description={fact.description}
            image={fact.image}
          />
        ))
      }
    </div>
  </main>
</Layout>
